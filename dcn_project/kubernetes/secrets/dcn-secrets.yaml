apiVersion: v1
kind: Secret
metadata:
  name: dcn-secrets
  namespace: dcn
  labels:
    app: dcn
type: Opaque
stringData:
  # ONTAP Credentials
  ONTAP_USERNAME: "admin"
  ONTAP_PASSWORD: "changeme"

  # StorageGRID Credentials
  STORAGEGRID_USERNAME: "admin"
  STORAGEGRID_PASSWORD: "changeme"

  # Database Credentials
  POSTGRES_USER: "dcn"
  POSTGRES_PASSWORD: "dcnpass"
  POSTGRES_DB: "dcn"

  # Redis Password (if needed)
  REDIS_PASSWORD: ""

  # API Keys
  API_SECRET_KEY: "your-secret-key-change-in-production"
  JWT_SECRET: "your-jwt-secret-change-in-production"
